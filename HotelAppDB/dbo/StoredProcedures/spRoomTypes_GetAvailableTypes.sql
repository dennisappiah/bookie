CREATE PROCEDURE [dbo].[spRoomTypes_GetAvailableTypes]
	@startDate date,
	@endDate date
AS
begin
	set nocount on;

	-- when there is no booking done
	-- // select Id, Title, Description, Price from dbo.RoomTypes
	

	-- Available Rooms after a booking

end

